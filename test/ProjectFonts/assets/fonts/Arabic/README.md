# Arabic

`NotoSansArabic-Subset.ttf` is a test-only subset of Noto Sans Arabic from
the archived `notofonts/noto-fonts` hinted TTF distribution. It retains the
characters and OpenType layout closure required to shape `سلام`.

The font is distributed under the SIL Open Font License 1.1 in `LICENSE.txt`.
