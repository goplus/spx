
onKey KeyC, => {
	turn 45
}
onKey KeyE, => {
	step 5
}

onKey KeyUp, => {
	Game.Camera.ChangeXYpos(0,5.0)
}

onKey KeyDown, => {
	Game.Camera.ChangeXYpos(0,-5.0)
}

onKey KeyLeft, => {
	Game.Camera.ChangeXYpos(-5.0,0.0)
}

onKey KeyRight, => {
	Game.Camera.ChangeXYpos(0.0,5.0)
}
