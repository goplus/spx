onClick => {
	animate "clap"
}

onKey KeySpace, => {
	animate "think"
}

onKey KeyUp, => {
	prevCostume
	say costumeIndex
}

onKey KeyDown, => {
	nextCostume
	say costumeIndex
}

onKey [KeyEnter, KeyS], => {
	step 5
	step -5
}

onKey KeyLeft, => {
	step 5
}

onKey KeyRight, => {
	step -5
}
