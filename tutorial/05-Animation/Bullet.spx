onTouchStart => {
	destroy
}

onStart => {
	for {
		wait 0.3
		clone
	}
}

onCloned => {
	setXYpos mouseX, mouseY
	show
	for {
		waitNextFrame
		step 250 * deltaTime
		if touching(Edge) {
			destroy
		}
	}
}