onTouchStart => {
	destroy
}

onStart => {
	for {
		wait 0.1
		clone
	}
}

onCloned => {
	setXYpos mouseX, mouseY
	show
	for {
		waitNextFrame
		step 550 * deltaTime
		if touching(Edge) {
			destroy
		}
	}
}